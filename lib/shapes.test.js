const { Circle, Triangle, Square } = require('./shapes');

describe('Circle class', () => {
  test('draw method should draw a circle', () => {
    
  });
});

describe('Triangle class', () => {
  test('draw method should draw a triangle', () => {
    
  });
});

describe('Square class', () => {
  test('draw method should draw a square', () => {
    
  });
});
